language: python
python:
#  - "2.6" # Needs LXML or html5lib
  - "2.7"
  - "3.2"
  - "3.3"
# command to install dependencies
install:
  - "pip install -r requirements.txt"
  - "pip install -r test-requirements.txt"
# command to run tests
script:
  - flake8 *.py
  - nosetests
